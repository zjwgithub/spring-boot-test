spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/spring-boot-test
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create
logging:
  level:
    root: info
    org.hibernate.SQL: debug
